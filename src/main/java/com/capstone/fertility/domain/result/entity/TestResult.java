package com.capstone.fertility.domain.result.entity;

/**
 * 현재 미사용. 검사 결과는 완료된 TestSession으로 표현하며,
 * 상세 조회는 GET /results/{resultId} (resultId = sessionId)로 제공합니다.
 * 향후 결과를 별도 테이블로 분리할 경우 사용 예정.
 */
@Deprecated
public class TestResult {
}

